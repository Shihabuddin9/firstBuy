import useTitle from "../../../hooks/UseTitle/UseTitle";

const MyOrders = () => {
    useTitle('My Orders')
    return (
        <div>
            <h1>My order</h1>
        </div>
    );
};

export default MyOrders;